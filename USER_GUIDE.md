# Emulator Auto Updater 상세 사용 설명서

**Emulator Auto Updater**는 자동 업데이트 기능이 없거나 최신 나이트리(Nightly)/개발(Dev) 빌드를 주기적으로 다운로드해야 하는 에뮬레이터들을 통합 관리하고, 버튼 클릭 한 번으로 병렬 조회, 고속 다운로드 및 자동 압축 해제까지 한 번에 처리해 주는 .NET 8 WPF 데스크톱 프로그램입니다.

---

## 📋 목차
1. [주요 특징](#-주요-특징)
2. [화면 구성 및 디자인 설명](#-화면-구성-및-디자인-설명)
3. [단계별 상세 사용법](#-단계별-상세-사용법)
   - [3.1 에뮬레이터 추가 및 기본 설정](#31-에뮬레이터-추가-및-기본-설정)
   - [3.2 Asset Pattern(정규식) 작성, 제외 패턴 및 자동 변환기 사용법](#32-asset-pattern정규식-작성-제외-패턴-및-자동-변환기-사용법)
   - [3.3 개별 및 전체 업데이트 확인 (최대 10개 병렬)](#33-개별-및-전체-업데이트-확인-최대-10개-병렬)
   - [3.4 고속다운로드 및 압축 해제 (8분할 병렬 엔진)](#34-고속다운로드-및-압축-해제-8분할-병렬-엔진)
   - [3.5 버전별 / Release 날짜별 하위 폴더 분리 저장 옵션](#35-버전별--release-날짜별-하위-폴더-분리-저장-옵션)
   - [3.6 실행 중인 에뮬레이터 자동 감지 및 덮어쓰기 보호](#36-실행-중인-에뮬레이터-자동-감지-및-덮어쓰기-보호)
   - [3.7 실시간 작업 현황 및 자동 스크롤 로그 콘솔](#37-실시간-작업-현황-및-자동-스크롤-로그-콘솔)
   - [3.8 설정 파일(config.json) 독립 관리](#38-설정-파일configjson-독립-관리)
4. [주요 에뮬레이터별 추천 설정 표](#-주요-에뮬레이터별-추천-설정-표)
5. [자주 묻는 질문 (FAQ) 및 문제 해결](#-자주-묻는-질문-faq-및-문제-해결)

---

## ✨ 주요 특징

- ⚡ **자체 프로그램 자동 업데이트 엔진 (`v1.2.0`)**: 프로그램 기동 시 GitHub Releases 저장소를 조회하여 새 버전 출시 시 상단 툴바에 `⚡ 프로그램 업데이트 가능` 버튼이 출현하며, 클릭 한 번으로 자동 다운로드 ➔ 자가 교체 ➔ 앱 자동 재기동까지 원클릭으로 진행됩니다.
- 🎨 **모던 슬레이트 & 카드 UI**: 심미적이고 정돈된 카드 패널 구조와 시각적 상태 배지 적용. 창 크기에 맞춘 동적 비율 조정으로 기동 시 하단 스크롤바가 생기지 않습니다.
- 🏷️ **직관적인 상태 배지 (Pill Badge) & 행 하이라이트**: 에뮬레이터 목록에서 `🟢 ⚡ 업데이트 가능`, `🔵 ✓ 최신 버전`, `🔴 ⚠️ 조회 실패`, `🟡 🔍 확인 중...`, `🟣 ⬇️ 다운로드 중...`, `⚪ ➖ 미확인` 상태를 한눈에 식별 가능하며, 업데이트 가능 항목 및 조회 실패 항목에 소프트 파스텔 배경색이 자동 적용됩니다.
- 📂 **버전별 / Release 날짜별 하위 폴더 자동 분리 저장**: 특정 구버전이 성능이나 호환성이 더 좋을 때를 대비하여, 상단 `[x] 버전/날짜 하위 폴더에 저장` 옵션을 키면 지정 설치 폴더 내에 버전명(예: `2.7.494`) 또는 Release 게시 날짜(예: `2026-07-28_10-55`)로 하위 폴더를 자동 생성하여 다운로드 및 압축 해제합니다.
- 🛑 **실행 중인 에뮬레이터 자동 감지 및 보호 (Process Lock Guard)**: 사용자가 게임을 즐기는 도중 실수로 업데이트를 실행하더라도, 실행 중인 `.exe` 프로세스를 사전에 자동 감지하여 파일 덮어쓰기 오류(UnauthorizedAccessException)나 파일 파손 없이 안전하게 작업 중단 안내를 띄워 줍니다.
- 🔄 **기동 시 '미확인' 상태 자동 초기화**: 프로그램 재기동 시 이전의 조회 결과로 오해하지 않도록 일회성 UI 상태를 `config.json`에 저장하지 않고 깔끔하게 `⚪ ➖ 미확인`으로 시작합니다.
- 📜 **실시간 타임스탬프 작업 로그 콘솔**: 작업 내역, 다운로드 현황, 시스템 오류 및 예외(Exception)까지 `[HH:mm:ss]` 시각과 함께 기록되는 멀티라인 스크롤 콘솔 지원 (`[로그 지우기]` 및 자동 스크롤 기능 제공).
- 🔍 **조회 방식 투명 제공**: REST API 사용 여부 또는 웹 파싱 우회 사용 여부를 하단 로그 및 릴리즈 노트 상단(`[조회 방식: GitHub REST API]`, `[조회 방식: 웹 파싱 (latest 리다이렉트)]` 등)에 명확하게 투명 표시.
- 🎯 **AssetPattern 기반 타겟 시스템 정밀 추적**: 사용자 지정 정규식(예: Windows x64)을 릴리즈 탐색 단계에서 사전 검증하여, 최신 릴리즈에 내 OS용 파일이 없더라도 다른 릴리즈 태그를 순회 탐색하여 내 PC용 최신 빌드를 정밀 추적합니다.
- ⚡ **최대 10개 병렬 조회 및 다운로드**: 모든 에뮬레이터의 최신 업데이트를 하나씩 기다릴 필요 없이 동시 병렬 
- 🚀 **8분할 고속 병렬 다운로드 엔진**: HTTP Range 요청 헤더를 활용해 5MB 이상의 대용량 아티팩트를 8개 커넥션으로 분할 다운로드하여 기존 속도 대비 8~10배 이상 초고속 다운로드 (Azure Blob / GitHub CDN / nightly.link 지원).
- 🔓 **GitHub API Rate Limit 우회 & 봇 보호 해제**: GitHub REST API 호출 횟수 제한(60회/시간) 도달 시 웹 HTML 우회 파서(`expanded_assets`)로 자동 전환되며, `/releases/latest` 리다이렉트 우선 조회 엔진으로 RPCS3(`0.0.41-19607`), PCSX2 나이트리 등 최신 빌드를 100% 정확하게 수신합니다.
- 📅 **버전 미표시 파일 게시 일자 추적 Engine**: 파일명이나 태그에 별도의 버전 번호가 없는 고정 파일명(예: `BizHawk-dev-windows.zip`, `Flycast-win64.zip`, `desmume-win-x64.zip`, `Mesen.zip` 등)은 **게시 일시(`yyyy-MM-dd HH:mm`)를 버전으로 자동 생성 및 관리**합니다. 사전 조회 없이 바로 `[다운로드]`를 누르더라도 정확한 커밋/릴리즈 타임스탬프를 읽어와 기록합니다.
- 🪄 **스마트 Asset Pattern 자동 변환기**: 정규식을 몰라도 예시 파일명(예: `PPSSPPWindows64_1.17.1.zip`)을 입력하고 `[패턴 자동 변환]`을 누르면 완벽한 정규식으로 자동 전환.
- 📁 **완벽한 포터블(Portable) 설정 지원**: 실행 폴더 내 `config.json`을 우선적으로 읽어오며, 필요 시 다른 이름으로 설정 저장 및 불러오기 가능.
- 🛠️ **안전한 원자적 파일 추출**: 다운로드 및 압축 해제 중 실패 시 원본 폴더를 오염시키지 않으며 완료된 후 기존 압축 파일 자동 정리.

---

## 🖥️ 화면 구성 및 디자인 설명

### 상단 제어 바 (Global Toolbar)
- **`에뮬레이터 추가`**: 새로운 에뮬레이터 관리 항목을 목록에 추가합니다.확인 및 동시 다운로드.
- **`선택 삭제`**: 목록에서 선택한 에뮬레이터 항목을 제거합니다 (진행 중인 작업이 없을 때 가능).
- **`설정 불러오기`**: 지정된 설정 파일(`config.json`)을 다시 읽어옵니다.
- **`설정 저장`**: 현재 변경된 에뮬레이터 목록 및 프로그램 설정을 저장합니다.
- **`설정 파일 열기...`**: 다른 위치에 있는 `config.json` 설정 파일을 열어 로드합니다.
- **`다른 이름으로 저장...`**: 현재 설정을 원하는 폴더에 새로운 파일명으로 저장합니다.
- **`전체 업데이트 확인`**: 목록에 있는 모든 에뮬레이터를 최대 10개씩 동시 병렬로 최신 빌드 유무를 확인합니다.
- **`전체 다운로드`**: 전체 업데이트 확인을 우선 수행한 후, 최신 빌드가 존재하는 항목들을 병렬로 자동 다운로드 및 압축 해제합니다 (이미 최신 버전인 항목은 자동 스킵).
- **`기동 시 전체 업데이트 확인`**: 프로그램 실행 시 자동으로 전체 업데이트 조회를 수행할지 여부를 설정합니다.
- **`버전/날짜 하위 폴더에 저장`**: 설정 시 지정 폴더 내에 버전명(예: `2.7.494`) 또는 Release 날짜(예: `2026-07-28_10-55`)로 하위 폴더를 만들어 분리 저장합니다.
- **`기본 Asset Pattern`**: 새 에뮬레이터를 추가할 때 기본으로 적용될 패턴 정규식입니다.
- **`패턴 변환`**: 기본 Asset Pattern 칸에 적힌 파일명을 정규식으로 변환합니다.

### 좌측 에뮬레이터 목록 (DataGrid)
- **`에뮬레이터 이름`**: 에뮬레이터 식별 명칭.
- **`상태 (Status)`**: 
  - `🟢 ⚡ 업데이트 가능 (버전명)` : 루트 폴더 및 하위 폴더에 최신 빌드가 없음 (행 배경: 소프트 연초록 `#F0FDF4`).
  - `🔵 ✓ 최신 (루트+하위폴더)` : 루트 폴더와 버전 하위 폴더 모두 최신 빌드로 완비되어 있음.
  - `🔵 ✓ 최신 (하위폴더: 버전명)` : 루트 폴더는 구버전이나, 디스크 하위 폴더에 최신 빌드가 감지됨.
  - `🔵 ✓ 최신 (루트폴더)` : 메인 루트 폴더에 최신 빌드가 설치되어 있음.
  - `🔴 ⚠️ 조회 실패` : URL 오류 또는 네트워크 실패 (행 배경: 소프트 연분홍 `#FEF2F2`).
  - `🟡 🔍 확인 중...` : 백그라운드에서 최신 릴리즈를 확인하는 중.
  - `🟣 ⬇️ 다운로드 중...` : 고속 다운로드 및 압축 해제가 진행되는 중.
  - `⚪ ➖ 미확인` : 조회가 실행되지 않은 초기 상태.
- **`Repository / URL`**: GitHub 저장소(`owner/repo`), API URL, 또는 웹 페이지 주소.
- **`설치 폴더 경로`**: 에뮬레이터 실행 파일이 설치된 폴더 경로.
- **`Asset Pattern (정규식)`**: 대상 아티팩트를 필터링하는 정규식.

### 우측 상단 상세 정보 및 작업 창 (Detail View)
- **`이름` / `Repository` / `설치 폴더` / `Asset Pattern`**: 선택한 항목의 상세 설정.
- **`[폴더 선택]`**: 탐색기 창을 열어 에뮬레이터 설치 폴더 지정.
- **`[패턴 자동 변환]`**: 입력한 파일명을 추천 정규식으로 자동 전환.
- **`[업데이트 확인]`**: 선택한 에뮬레이터 개별 건에 대해 백그라운드에서 최신 릴리즈 조회.
- **`[다운로드]`**: 선택한 릴리즈 아티팩트를 압축 해제 없이 폴더에 다운로드만 수행.
- **`[다운로드 + 압축 해제]`**: 최신 아티팩트를 고속 다운로드 후 해당 설치 폴더에 압축을 해제하고 압축 파일 자동 정리.
- **`[📁 하위 폴더 관리]`**: 해당 에뮬레이터의 하위 저장 폴더 전용 관리 팝업창을 엽니다.
- **`릴리즈 노트`**: 조회된 최신 빌드의 버전 태그, 커밋 해시, 변경 사항(Changelog) 및 **`[조회 방식]`** 헤더 정보 표시.

### 📁 다운로드된 버전 폴더 관리 팝업 (Subfolder Manager Window)
- **진입 방법**:
  1. 에뮬레이터 목록 행을 **우클릭** ➔ `📁 다운로드된 버전 폴더 관리...` 선택.
  2. 우측 제어 패널에서 **`[📁 하위 폴더 관리]`** 버튼 클릭.
- **주요 기능**:
  - **스마트 버전 폴더 필터링 (Smart Version Subfolder Filter)**:
    - 무분별한 하위 폴더 노출을 방지하기 위해 사용자 데이터 폴더(`bios`, `saves`, `cheats`, `shaders`, `textures`, `memcards` 등)를 자동 제외합니다.
    - 실제 `.exe` 실행 파일이 포함되어 있거나, 버전 규칙(예: `2.7.494`, `v1.18.0`) 또는 릴리즈 일자 규칙(예: `2026-07-28_10-55`)에 부합하는 **정식 에뮬레이터 버전 하위 폴더만 엄격하게 검증하여 리스팅**합니다.
    - 조건에 부합하는 폴더가 없을 경우 `"다운로드된 버전 하위 폴더를 찾을 수 없습니다."` 안내가 표시됩니다.
  - **다중 선택 지원**: 각 버전 폴더별 체크박스(`[x]`)로 원하는 폴더들을 다중 선택 가능.
  - **`전체 선택` / `전체 해제`**: 목록 내 모든 하위 폴더를 한 번에 선택/해제.
  - **상세 정보 제공**: 폴더명(버전/날짜), 내부 `.exe` 실행 파일 유무, 디스크 차지 용량(MB/GB), 생성/수정 일시 표시.
  - **선택 용량 자동 계산**: 선택된 폴더들의 합산 디스크 확보 예상 용량 자동 계산 표시.
  - **안전 삭제 2중 가드**:
    - **프로세스 잠금 검사**: 실행 중인 에뮬레이터(`.exe`)가 있는 경우 삭제를 차단하고 경고 팝업 안내.
    - **영구 삭제 확인 창**: 실수 삭제 방지를 위해 대상 폴더명 및 용량을 보여주는 확인 창 팝업.

### 하단 조회된 다운로드 파일 목록 (Downloadable Assets List)
- **`[📋 파일명 복사]`**: 패치 목록에서 선택한 파일명을 즉시 윈도우 클립보드에 복사합니다.
- **`[🪄 패턴에 반영]`**: 선택한 파일명을 추천 Asset Pattern 정규식으로 자동 전환하여 적용합니다.
- **`[🚫 패턴에서 제외]`**: 제외 대상 파일(예: `pcsx2-v2.6.3-windows-x64-Qt-symbols.7z` 등)을 선택하고 누르면 불필요한 키워드(`symbols`)를 분석하여 패턴에 부정 전방 탐색 제외 조건(`(?!.*symbols)`)을 자동으로 추가합니다.
- **우클릭 컨텍스트 메뉴**: 파일 목록 행을 우클릭하여 `📋 파일명 복사`, `🪄 Asset Pattern으로 복사 & 자동 변환`, `🚫 패턴에서 제외 조건 추가` 메뉴를 이용할 수 있습니다.

### 우측 하단 작업 현황 및 로그 콘솔 (Activity Log Console)
- **프로그레스 바**: 진행 중인 다운로드/압축해제 작업의 진행률(0~100%) 표시.
- **로그 콘솔 (`TextBox`)**: 
  - 조작 내역, 다운로드 단계별 현황, 압축 해제 결과, 실행 방식(API/웹파싱) 및 예외(Exception)가 타임스탬프(`[HH:mm:ss]`)와 함께 실시간으로 출력됩니다.
  - 새 로그가 들어오면 **자동으로 최하단으로 스크롤**됩니다.
- **`[로그 지우기]`**: 콘솔 내 기록된 로그 텍스트를 초기화합니다.
- **설정 파일 경로**: 현재 활성화되어 읽고 쓰고 있는 `config.json` 파일의 전체 경로 표시.

---

## 📖 단계별 상세 사용법

### 3.1 에뮬레이터 추가 및 기본 설정

1. 상단 **`[에뮬레이터 추가]`** 버튼을 누릅니다.
2. 목록 우측 상세 창에서 에뮬레이터의 **이름**을 입력합니다 (예: `PPSSPP`).
3. **`[폴더 선택]`** 버튼을 눌러 실제 에뮬레이터 실행 파일(`PPSSPPWindows64.exe` 등)이 위치한 폴더를 지정합니다. (폴더가 아직 없더라도 압축 해제 시 자동 생성됩니다)
4. **Repository / URL** 입력란에 해당 에뮬레이터의 GitHub 주소(예: `hrydgard/ppsspp`) 또는 웹사이트 주소를 입력합니다.

---

### 3.2 Asset Pattern(정규식) 작성, 제외 패턴 및 자동 변환기 사용법

에뮬레이터 배포 페이지에는 Windows용, macOS용, Android용 등 여러 파일이 함께 올라옵니다. 이 중 **내 PC에 맞는 Windows 64비트 Zip 파일**만 골라내기 위해 Asset Pattern을 사용합니다.

#### 🪄 스마트 자동 변환기 사용법 (추천)
1. **Asset Pattern** 입력란에 배포 사이트에서 볼 수 있는 **실제 파일명 예시**를 그대로 입력하거나 붙여넣습니다.
   - 예시 1: `PPSSPPWindows64_v1.18.0.zip`
   - 예시 2: `Eden-Windows-0133caf702-amd64-clang-pgo.zip`
   - 예시 3: `dolphin-master-5.0-21430-x64.7z`
2. 바로 옆의 **`[패턴 자동 변환]`** 버튼을 누릅니다.
3. 파일명 내의 버전 숫자나 커밋 해시는 와일드카드(`.*`)로 치환되고, OS(`win/windows`), 아키텍처(`x64/amd64`), 확장자(`.zip/.7z`) 순서가 원본 파일 순서대로 유지된 호환성 높은 정규식으로 자동 전환됩니다.

#### 🚫 원치 않는 파일(symbols, debug, ssl 등) 1초 만에 패턴에서 제외하기 (신규 기능)
1. 하단 **`[조회된 다운로드 파일 목록]`**에서 제외하고 싶은 파일(예: `pcsx2-v2.6.3-windows-x64-Qt-symbols.7z`)을 선택합니다.
2. 목록 우측 상단의 **`[🚫 패턴에서 제외]`** 버튼(또는 마우스 우클릭 메뉴)을 클릭합니다.
3. 기존 정규식 패턴에 부정 전방 탐색 구문 `(?!.*symbols)`이 자동으로 주입되어, 디버그 심볼이나 보조 파일이 완벽하게 차단됩니다.

---

### 3.3 개별 및 전체 업데이트 확인 (최대 10개 병렬)

- **개별 확인**: 목록에서 에뮬레이터를 선택한 후 **`[업데이트 확인]`**을 누르면 백그라운드에서 해당 건만 조회됩니다.
- **전체 확인**: 상단 **`[전체 업데이트 확인]`**을 누르면 모든 에뮬레이터의 업데이트 상태를 최대 10개씩 동시 병렬로 초고속 조회합니다.

---

### 3.4 고속다운로드 및 압축 해제 (8분할 병렬 엔진)

1. 최신 빌드가 발견되면 **`[다운로드 + 압축 해제]`** 버튼을 누릅니다.
2. **8분할 병렬 다운로드 엔진**: 파일 크기가 5MB 이상인 경우 자동으로 8개 커넥션으로 분할 다운로드하여 초고속으로 수신합니다.
3. 다운로드가 완료되면 설정된 에뮬레이터 폴더에 안전하게 압축을 해제하고 임시 압축 파일은 자동 삭제됩니다.

---

### 3.5 버전별 / Release 날짜별 하위 폴더 분리 저장 옵션

특정 에뮬레이터의 이전 버전이나 특정 빌드를 독립적으로 보존하여 사용하고 싶다면 상단 툴바의 **`[x] 버전/날짜 하위 폴더에 저장`** 옵션을 체크하세요.

- **버전 정보가 있는 에뮬레이터** (PCSX2, RPCS3, Ryujinx 등):
  - 지정한 폴더 내에 버전 태그명(예: `G:\Emulators\PCSX2\v2.7.494\`)으로 하위 폴더를 자동 생성하여 해당 폴더 내에 압축을 해제합니다.
- **버전 정보가 없는 고정 파일명 에뮬레이터** (DeSmuME, Mesen, BizHawk 등):
  - 지정한 폴더 내에 커밋/릴리즈 게시 날짜(예: `G:\Emulators\DeSmuME\2026-07-28_10-55\`)로 하위 폴더를 생성하여 저장합니다.

---

### 3.6 실행 중인 에뮬레이터 자동 감지 및 덮어쓰기 보호

게임 플레이 도중 업데이트를 시도하더라도 안심할 수 있습니다.
- 업데이터가 압축을 해제하기 직전에 해당 설치 폴더에서 실행 중인 프로세스(`.exe`)가 있는지 사전에 자동으로 감지합니다.
- 실행 중인 에뮬레이터가 발견되면 작업을 즉시 중단하고 `에뮬레이터 프로세스(PCSX2.exe)가 실행 중입니다. 에뮬레이터를 종료한 후 다시 시도하세요.` 안내 메시지를 출력하여 **파일 파손이나 시스템 락 오류를 100% 방지**합니다.

---

### 3.7 실시간 작업 현황 및 자동 스크롤 로그 콘솔

- **통합 로그 콘솔**: 작업 진행 상황, 다운로드 상태, 압축 해제 경로 및 예외가 타임스탬프(`[HH:mm:ss]`)와 함께 실시간으로 기록됩니다.
- **자동 스크롤**: 새 로그가 들어오면 최하단으로 자동 스크롤됩니다.

---

### 3.8 설정 파일(config.json) 독립 관리

- **포터블 모드**: 실행 파일 폴더 내의 `config.json`을 우선 관리하여 USB 등 이동식 디스크에서도 설정이 그대로 보존됩니다.

---

## 🌐 주요 에뮬레이터별 추천 설정 표

| 에뮬레이터 | Repository / URL 주소 | 추천 Asset Pattern 정규식 |
| :--- | :--- | :--- |
| **RPCS3** (PS3) | `RPCS3/rpcs3-binaries-win` | `(?i)rpcs3.*(win64\|msvc).*\.(7z\|zip)$` |
| **PCSX2** (PS2) | `PCSX2/pcsx2` | `(?i)(?!.*symbol)pcsx2.*(win\|windows).*(x64\|amd64).*\.(zip\|7z)$` |
| **AzaharPlus** (3DS) | `AzaharPlus/AzaharPlus` | `(?i)azaharplus.*windows.*\.(zip\|7z)$` |
| **Dolphin** (Wii/GameCube) | `https://dolphin-emu.org/download/` | `(?i)dolphin.*(x64\|amd64\|64).*\.(zip\|7z)$` |
| **PPSSPP** (PSP) | `https://www.ppsspp.org/devbuilds/` | `(?i)ppsspp.*(win\|windows).*\.(zip\|7z)$` |
| **DuckStation** (PS1) | `stenzek/duckstation` | `(?i)(?!.*(ssl\|symbol))duckstation.*(win\|windows).*(x64).*\.(zip)$` |
| **BizHawk** (다기능 TAS) | `TASEmulators/BizHawk` 또는 `https://nightly.link/TASEmulators/BizHawk/workflows/ci/master/BizHawk-dev-windows.zip` | `(?i)BizHawk.*(win\|windows).*\.(zip\|7z)$` |
| **Eden** (Switch) | `https://git.eden-emu.dev/api/v1/repos/eden-ci/nightly/releases/latest` | `(?i)Eden.*(win\|windows).*(amd64).*(clang).*\.(zip\|7z)$` |
| **Ryujinx** (Switch) | `https://git.ryujinx.app/api/v1/repos/ryubing/canary/releases/latest` | `(?i)ryujinx.*win_x64.*\.(zip\|7z)$` |
| **melonDS** (NDS) | `https://melonds.kuribo64.net/nightlies.php` | `(?i)melonDS.*(win\|windows).*\.(zip\|7z)$` |
| **Flycast** (Dreamcast) | `https://flyinghead.github.io/flycast-builds/` | `(?i)flycast.*(win\|windows).*\.(zip\|7z)$` |
| **Vita3K** (PS Vita) | `Vita3K/Vita3K-builds` | `(?i)vita3k.*windows.*\.(zip\|7z)$` |
| **Citron NEO** (Switch) | `citron-neo/CI` | `(?i)Citron.*(win\|windows).*x64.*\.(zip\|7z)$` |

---

## ❓ 자주 묻는 질문 (FAQ) 및 문제 해결

### Q1. GitHub API 횟수 제한(Rate Limit Exceeded: 403) 에러가 발생해요.
- **해결책**: 본 프로그램에는 **GitHub API 횟수 제한 자동 우회 파서**가 내장되어 있어 API 제한 도달 시 자동으로 웹 HTML 및 `expanded_assets` 파서로 전환됩니다. 사용자가 별도로 조치할 필요 없이 조회가 지속됩니다.

### Q2. RPCS3나 PCSX2 나이트리 빌드가 이상한 옛날 버전으로 조회돼요.
- **해결책**: 일부 저장소(RPCS3 등)는 태그를 커밋 해시로 지정하여 일반 목록 조회 시 이전 빌드가 반환될 수 있습니다. 본 프로그램은 `releases/latest` API 및 HTTP 리다이렉트 우선 조회 엔진이 적용되어 항상 **실제 최신 빌드(예: RPCS3 `0.0.41-19607` 등)**를 정확히 찾아냅니다.

### Q3. 특정 버전이나 릴리즈 날짜별로 별도 폴더에 모아서 보관하고 싶어요.
- **해결책**: 상단 툴바의 **`[x] 버전/날짜 하위 폴더에 저장`** 체크박스를 켜두시면 설치 폴더 내에 버전명 또는 게시 날짜별로 하위 폴더를 자동 생성하여 덮어쓰지 않고 안전하게 분리 저장해 줍니다.

### Q4. 게임 플레이 중에 실수로 다운로드를 누르면 기존 게임 파일이 깨지나요?
- **해결책**: **깨지지 않습니다.** 본 프로그램은 압축 해제 직전 에뮬레이터 프로세스가 켜져 있는지 감지하여, 실행 중인 경우 작업을 멈추고 사용자에게 종료 후 재시도할 것을 안전하게 안내해 줍니다.

### Q5. 압축 해제 후 기존에 내가 넣어둔 세이브 파일이나 설정이 지워지나요?
- **답변**: **아닙니다.** 본 프로그램은 에뮬레이터 릴리즈 압축 파일 안에 들어있는 파일만 안전하게 덮어쓰며, 사용자가 따로 넣어둔 세이브 데이터(`User/`, `saves/`, `portable.txt` 등)는 전혀 건드리지 않습니다.

---
*제작: Emulator Auto Updater Team (.NET 8 WPF)*
