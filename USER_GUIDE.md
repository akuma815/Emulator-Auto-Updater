# Emulator Auto Updater 상세 사용 설명서 (v1.3.0)

**Emulator Auto Updater**는 자동 업데이트 기능이 없거나 최신 나이트리(Nightly)/개발(Dev) 빌드를 주기적으로 다운로드해야 하는 에뮬레이터들을 통합 관리하고, 버튼 클릭 한 번으로 병렬 조회, 초고속 다운로드 및 자동 압축 해제까지 한 번에 처리해 주는 .NET 8 WPF 데스크톱 프로그램입니다.

---

## 📋 목차
1. [주요 특징](#-주요-특징)
2. [v1.3.0 새로운 기능 및 개선 사항](#-v130-새로운-기능-및-개선-사항)
3. [화면 구성 및 디자인 설명](#-화면-구성-및-디자인-설명)
4. [단계별 상세 사용법](#-단계별-상세-사용법)
   - [4.1 에뮬레이터 추가 및 기본 설정](#41-에뮬레이터-추가-및-기본-설정)
   - [4.2 Asset Pattern(정규식) 작성, 제외 패턴 및 자동 변환기 사용법](#42-asset-pattern정규식-작성-제외-패턴-및-자동-변환기-사용법)
   - [4.3 개별 및 전체 업데이트 확인 (최대 10개 병렬)](#43-개별-및-전체-업데이트-확인-최대-10개-병렬)
   - [4.4 초고속 다운로드 및 압축 해제 (8분할 병렬 엔진)](#44-초고속-다운로드-및-압축-해제-8분할-병렬-엔진)
   - [4.5 버전별 / Release 날짜별 하위 폴더 분리 저장 및 관리](#45-버전별--release-날짜별-하위-폴더-분리-저장-및-관리)
   - [4.6 실행 중인 에뮬레이터 자동 감지 및 덮어쓰기 보호](#46-실행-중인-에뮬레이터-자동-감지-및-덮어쓰기-보호)
   - [4.7 실시간 작업 현황 및 다국어 로그 콘솔](#47-실시간-작업-현황-및-다국어-로그-콘솔)
   - [4.8 설정 파일(config.json) 독립 관리](#48-설정-파일configjson-독립-관리)
5. [주요 에뮬레이터별 추천 설정 표](#-주요-에뮬레이터별-추천-설정-표)
6. [자주 묻는 질문 (FAQ) 및 문제 해결](#-자주-묻는-질문-faq-및-문제-해결)

---

## ✨ 주요 특징

- 🌐 **완벽한 전역 다국어 지원 (Full Localization)**: **한국어(Korean)**, **English**, **日本語(Japanese)**, **简体中文(Simplified Chinese)** 4개 언어를 완벽 지원합니다. UI 텍스트뿐만 아니라 DataGrid 컬럼 헤더, 상태 배지, 하단 실시간 작업 로그 콘솔, 하위 폴더 관리자 화면 및 각종 대화상자까지 선택된 언어로 즉각 동기화됩니다.
- 🐬 **돌핀(Dolphin) 공식 CI Buildbot REST API 직접 연동**: 최근 `dolphin-emu.org` 웹사이트에 도입된 Bunny Shield(CDN 봇 챌린지 및 403 차단)를 완벽 우회하고, 공식 Dolphin CI Buildbot REST API를 직접 연동하여 0.2초 이내로 초고속 마스터 개발 빌드(`dolphin-master-xxxx-x64.7z`)를 정확히 조회합니다.
- ⚡ **자체 프로그램 자동 업데이트 엔진 (`v1.3.0`)**: 프로그램 기동 시 GitHub Releases 저장소를 조회하여 새 버전 출시 시 상단 툴바에 `⚡ 프로그램 업데이트 가능` 버튼이 출현하며, 클릭 한 번으로 자동 다운로드 ➔ 자가 교체 ➔ 앱 자동 재기동까지 원클릭으로 진행됩니다.
- 🎨 **모던 슬레이트 & 카드 UI**: 심미적이고 정돈된 카드 패널 구조와 시각적 상태 배지 적용. 창 크기에 맞춘 동적 비율 조정으로 기동 시 불필요한 스크롤바가 생기지 않습니다.
- 🏷️ **직관적인 상태 배지 (Pill Badge) & 행 하이라이트**: 에뮬레이터 목록에서 `🟢 ⚡ 업데이트 가능`, `🔵 ✓ 최신 버전`, `🔴 ⚠️ 조회 실패`, `🟡 🔍 확인 중...`, `🟣 ⬇️ 다운로드 중...`, `⚪ ➖ 미확인` 상태를 한눈에 식별 가능하며, 업데이트 가능 항목 및 조회 실패 항목에 소프트 파스텔 배경색이 자동 적용됩니다.
- 📂 **버전별 / Release 날짜별 하위 폴더 자동 분리 저장 및 관리자 탑재**: 특정 구버전이 성능이나 호환성이 더 좋을 때를 대비하여, 상단 `[x] 버전/날짜 하위 폴더에 저장` 옵션을 켜면 지정 설치 폴더 내에 버전명(예: `2.7.524`) 또는 Release 게시 날짜(예: `2026-08-22_11-55`)로 하위 폴더를 자동 생성하여 다운로드 및 압축 해제합니다.
- 🛑 **실행 중인 에뮬레이터 자동 감지 및 보호 (Process Lock Guard)**: 사용자가 게임을 즐기는 도중 실수로 업데이트를 실행하더라도, 실행 중인 `.exe` 프로세스를 사전에 자동 감지하여 파일 덮어쓰기 오류(UnauthorizedAccessException)나 파일 파손 없이 안전하게 작업 중단 안내를 띄워 줍니다.
- 🔄 **기동 시 '미확인' 상태 자동 초기화**: 프로그램 재기동 시 이전의 조회 결과로 오해하지 않도록 일회성 UI 상태를 `config.json`에 저장하지 않고 깔끔하게 `⚪ ➖ 미확인`으로 시작합니다.
- 📜 **실시간 타임스탬프 다국어 작업 로그 콘솔**: 작업 내역, 다운로드 현황, 시스템 오류 및 예외까지 `[HH:mm:ss]` 시각과 함께 선택된 언어로 기록되는 멀티라인 스크롤 콘솔 지원 (`[로그 지우기]` 및 언어 변경 시 자동 초기화 기능 제공).
- 🔍 **조회 방식 투명 제공**: REST API 사용 여부 또는 웹 파싱 우회 사용 여부를 하단 로그 및 릴리즈 노트 상단(`[Source: GitHub REST API]`, `[Source: Dolphin CI]`, `[Source: Web (Release Page)]` 등)에 명확하게 투명 표시.
- 🎯 **AssetPattern 기반 타겟 시스템 정밀 추적**: 사용자 지정 정규식(예: Windows x64)을 릴리즈 탐색 단계에서 사전 검증하여, 최신 릴리즈에 내 OS용 파일이 없더라도 다른 릴리즈 태그를 순회 탐색하여 내 PC용 최신 빌드를 정밀 추적합니다.
- ⚡ **최대 10개 병렬 조회 및 다운로드**: 모든 에뮬레이터의 최신 업데이트를 하나씩 기다릴 필요 없이 동시 병렬 확인 및 동시 다운로드.
- 🚀 **8분할 고속 병렬 다운로드 엔진**: HTTP Range 요청 헤더를 활용해 5MB 이상의 대용량 아티팩트를 8개 커넥션으로 분할 다운로드하여 기존 속도 대비 8~10배 이상 초고속 다운로드 (Azure Blob / GitHub CDN / nightly.link 지원).
- 🔓 **GitHub API Rate Limit 우회 & 봇 보호 해제**: GitHub REST API 호출 횟수 제한(60회/시간) 도달 시 웹 HTML 우회 파서(`expanded_assets`)로 자동 전환되며, `/releases/latest` 리다이렉트 우선 조회 엔진으로 RPCS3, PCSX2 나이트리 등 최신 빌드를 100% 정확하게 수신합니다.
- 📅 **고정 태그 및 버전 미표시 파일 게시 일자 추적 Engine**: 태그명이 항상 동일하게 고정된 에뮬레이터(예: Duckstation의 `preview`, Citron의 `nightly-windows`, `latest`, `nightly` 등)나 파일명에 버전 번호가 없는 고정 파일명은 단순 태그명 일치로 인한 최신 버전 오탐을 완벽히 방지하고 **게시 일시(`yyyy-MM-dd HH:mm`) 및 마지막 다운로드 타임스탬프를 기준으로 최신 빌드 여부를 정밀 판정**합니다.
- 🪄 **스마트 Asset Pattern 자동 변환기**: 정규식을 몰라도 예시 파일명(예: `PPSSPPWindows64_1.17.1.zip`)을 입력하고 `[패턴 자동 변환]`을 누르면 완벽한 정규식으로 자동 전환.
- 📁 **완벽한 포터블(Portable) 설정 지원**: 실행 폴더 내 `config.json`을 우선적으로 읽어오며, 필요 시 다른 이름으로 설정 저장 및 불러오기 가능.

---

## 🚀 v1.3.0 새로운 기능 및 개선 사항

1. **완벽한 UI 및 로그 다국어 실시간 동기화**:
   - DataGrid의 컬럼 헤더가 WPF 바인딩 한계를 극복하는 전용 HeaderTemplate으로 개선되어 언어 변경 시 즉시 전환됩니다.
   - 작업 로그 콘솔, 상태 배지(`Checking...`, `Downloading...`, `Extracting...`), 대화상자까지 완벽하게 현지화되었습니다.
   - 언어 변경 시 기존 로그가 자동으로 초기화되어 언어가 뒤섞이지 않습니다.
   - 프로그램 기동 시 `config.json`의 언어를 조기에 감지하여 첫 시작 로그부터 설정된 언어로 표시됩니다.
2. **돌핀(Dolphin) 공식 CI Buildbot REST API 연동**:
   - `dolphin-emu.org` 웹사이트의 Bunny Shield CDN 보안 챌린지 403 차단 문제를 완전히 해결했습니다.
   - 공식 CI 빌드봇(`dolphin.ci`)의 REST API를 직접 연동하여 0.2초 이내로 최신 마스터 개발 빌드(`dolphin-master-xxxx-x64.7z`) 및 10개 최신 빌드 목록을 안정적으로 제공합니다.
3. **버전별 하위 폴더 관리자(Subfolder Manager) 전면 로컬라이즈 & 보호 강화**:
   - 버전 폴더 목록 조회, 크기 계산, 삭제 확인 및 프로세스 잠금 경고 메시지가 다국어로 제공됩니다.

---

## 🖥️ 화면 구성 및 디자인 설명

### 상단 제어 바 (Global Toolbar)
- **`🌐 언어 / Language`**: 한국어, English, 日本語, 简体中文 중 원하는 표시 언어를 실시간으로 선택합니다.
- **`에뮬레이터 추가`**: 새로운 에뮬레이터 관리 항목을 목록에 추가합니다.
- **`선택 삭제`**: 목록에서 선택한 에뮬레이터 항목을 제거합니다.
- **`설정 불러오기` / `설정 저장`**: 현재 `config.json` 설정 파일을 다시 읽거나 저장합니다.
- **`설정 파일 열기...` / `다른 이름으로 저장...`**: 외부 설정 파일을 로드하거나 다른 경로로 백업 저장합니다.
- **`전체 업데이트 확인`**: 목록에 있는 모든 에뮬레이터를 최대 10개씩 동시 병렬로 최신 빌드 유무를 확인합니다.
- **`전체 다운로드`**: 전체 업데이트 확인을 우선 수행한 후, 최신 빌드가 존재하는 항목들을 병렬로 자동 다운로드 및 압축 해제합니다 (이미 최신 버전인 항목은 자동 스킵).
- **`기동 시 전체 업데이트 확인`**: 프로그램 실행 시 자동으로 전체 업데이트 조회를 수행할지 여부를 설정합니다.
- **`버전/날짜 하위 폴더에 저장`**: 설정 시 지정 폴더 내에 버전명 또는 Release 날짜로 하위 폴더를 만들어 분리 저장합니다.
- **`기본 Asset Pattern` & `패턴 변환`**: 새 에뮬레이터를 추가할 때 기본 적용될 정규식을 설정하고 예시 파일명을 정규식으로 변환합니다.

### 좌측 에뮬레이터 목록 (DataGrid)
- **`에뮬레이터 이름`**: 에뮬레이터 식별 명칭.
- **`상태 (Status)`**: 
  - `🟢 ⚡ 업데이트 가능 (버전명)` : 루트 폴더 및 하위 폴더에 최신 빌드가 없음 (행 배경: 소프트 연초록 `#F0FDF4`).
  - `🔵 ✓ 최신 (루트+하위폴더)` : 루트 폴더와 버전 하위 폴더 모두 최신 빌드로 완비되어 있음.
  - `🔵 ✓ 최신 (하위폴더: 버전명)` : 루트 폴더는 구버전이나, 디스크 하위 폴더에 최신 빌드가 감지됨.
  - `🔵 ✓ 최신 (루트폴더)` : 메인 루트 폴더에 최신 빌드가 설치되어 있음.
  - `🔴 ⚠️ 조회 실패` : URL 오류 또는 네트워크 실패 (행 배경: 소프트 연분홍 `#FEF2F2`).
  - `🟡 🔍 확인 중...` : 백그라운드에서 최신 릴리즈를 확인하는 중.
  - `🟣 ⬇️ 다운로드 중...` : 고속 다운로드 및 압축 해제가 진행되는 중.
  - `⚪ ➖ 미확인` : 조회가 실행되지 않은 초기 상태.
- **`Repository / URL`**: GitHub 저장소(`owner/repo`), API URL, 또는 웹 페이지 주소.
- **`설치 폴더 경로`**: 에뮬레이터 실행 파일이 설치된 폴더 경로.
- **`Asset Pattern (정규식)`**: 대상 아티팩트를 필터링하는 정규식.

### 우측 상단 상세 정보 및 작업 창 (Detail View)
- **`이름` / `Repository` / `설치 폴더` / `Asset Pattern`**: 선택한 항목의 상세 설정.
- **`[폴더 선택]`**: 탐색기 창을 열어 에뮬레이터 설치 폴더 지정.
- **`[패턴 자동 변환]`**: 입력한 파일명을 추천 정규식으로 자동 전환.
- **`[업데이트 확인]`**: 선택한 에뮬레이터 개별 건에 대해 백그라운드에서 최신 릴리즈 조회.
- **`[다운로드]`**: 선택한 릴리즈 아티팩트를 압축 해제 없이 폴더에 다운로드만 수행.
- **`[다운로드 + 압축 해제]`**: 최신 아티팩트를 고속 다운로드 후 해당 설치 폴더에 압축을 해제하고 압축 파일 자동 정리.
- **`[📁 하위 폴더 관리]`**: 해당 에뮬레이터의 하위 저장 폴더 전용 관리 팝업창을 엽니다.
- **`릴리즈 노트`**: 조회된 최신 빌드의 버전 태그, 커밋 해시, 변경 사항(Changelog) 및 **`[조회 방식]`** 헤더 정보 표시.

### 📁 다운로드된 버전 폴더 관리 팝업 (Subfolder Manager Window)
- **진입 방법**:
  1. 에뮬레이터 목록 행을 **우클릭** ➔ `📁 다운로드된 버전 폴더 관리...` 선택.
  2. 우측 제어 패널에서 **`[📁 하위 폴더 관리]`** 버튼 클릭.
- **주요 기능**:
  - **스마트 버전 폴더 필터링**: 사용자 데이터 폴더(`bios`, `saves`, `cheats`, `shaders` 등)를 자동 보호하고 정식 에뮬레이터 버전 하위 폴더만 검증하여 리스팅합니다.
  - **다중 선택 및 용량 계산**: 체크박스로 여러 폴더를 선택하면 합산 확보 용량이 실시간 계산됩니다.
  - **안전 삭제 가드**: 실행 중인 프로세스 감지 및 영구 삭제 전 최종 확인 대화상자를 제공합니다.

---

## 📖 단계별 상세 사용법

### 4.1 에뮬레이터 추가 및 기본 설정
1. 상단 툴바의 **`+ 에뮬레이터 추가`** 버튼을 클릭합니다.
2. 우측 패널에서 **에뮬레이터 명**, **Repository / URL**, **설치 폴더 경로**를 지정합니다.
3. **`설정 저장`** 버튼을 눌러 `config.json`에 반영합니다.

### 4.2 Asset Pattern(정규식) 작성, 제외 패턴 및 자동 변환기 사용법
- **자동 변환**: `PPSSPPWindows64_1.17.1.zip` 같은 파일명을 입력하고 `[패턴 자동 변환]`을 누르면 추천 정규식이 완성됩니다.
- **하단 파일 목록 연동**: 
  - 파일명을 선택하고 **`🪄 패턴에 반영`**을 누르면 자동으로 정규식으로 변환되어 입력됩니다.
  - 원치 않는 파일(예: `symbols`, `debug`)을 선택하고 **`🚫 패턴에서 제외`**를 누르면 부정 전방 탐색 `(?!.*symbols)` 조건이 자동으로 추가됩니다.

### 4.3 개별 및 전체 업데이트 확인
- **개별 확인**: 에뮬레이터를 선택하고 **`업데이트 확인`** 클릭.
- **전체 확인**: 상단의 **`전체 업데이트 확인`** 클릭 (최대 10개 병렬 고속 검사).

### 4.4 초고속 다운로드 및 압축 해제
- **전체 다운로드**: 상단의 **`전체 다운로드`** 버튼 클릭 시 최신 빌드가 있는 에뮬레이터만 8분할 병렬 엔진으로 초고속 다운로드 및 자동 압축 해제됩니다.

---

## 🎮 주요 에뮬레이터별 추천 설정 표

| 에뮬레이터 | Repository / URL | 추천 Asset Pattern (정규식) |
|---|---|---|
| **Dolphin** | `https://dolphin-emu.org/download/` 또는 `https://dolphin.ci/` | `(?i)dolphin-master.*(x64|win).*\.7z$` |
| **PCSX2** | `https://github.com/PCSX2/pcsx2/tags` | `(?i)(?!.*symbols)pcsx2.*(win\|windows).*x64.*Qt.*\.7z$` |
| **RPCS3** | `https://github.com/RPCS3/rpcs3-binaries-win/releases` | `(?i)rpcs3.*win64_msvc.*\.7z$` |
| **PPSSPP (Dev)** | `https://www.ppsspp.org/devbuilds/` | `(?i)ppsspp_win_v1.*\.zip$` |
| **DuckStation** | `https://github.com/stenzek/duckstation/releases/tag/latest` | `(?i)(?!.*sse2)duckstation.*(win\|windows).*x64.*release.*\.zip$` |
| **Flycast (Dev)** | `https://flyinghead.github.io/flycast-builds/` | *(자동 인식)* |
| **melonDS (Dev)** | `https://melonds.kuribo64.net/nightlies.php` | *(자동 인식)* |
| **BizHawk (Dev)** | `https://nightly.link/TASEmulators/BizHawk/workflows/ci/master/BizHawk-dev-windows.zip` | *(자동 인식)* |
| **DeSmuME (Dev)** | `https://nightly.link/TASEmulators/desmume/workflows/build_win/master/desmume-win-x64.zip` | *(자동 인식)* |
| **Mesen (Dev)** | `https://nightly.link/nesdev-org/MesenCE/workflows/build/master/Mesen%20%28Windows%20-%20net10.0%20-%20AoT%29.zip` | *(자동 인식)* |
| **Eden (Nightly)** | `https://git.eden-emu.dev/api/v1/repos/eden-ci/nightly/releases/latest` | `(?i)Eden.*(win\|windows).*amd64.*clang.*pgo.*\.zip$` |
| **FB NEO** | `https://github.com/finalburnneo/FBNeo/releases` | `(?i)(?!.*debug).*(win\|windows).*x86_64.*\.zip$` |
| **Ryujinx** | `https://git.ryujinx.app/api/v1/repos/ryubing/canary/releases/latest` | `(?i)ryujinx.*canary.*win_x64.*\.zip$` |
| **Vita3K** | `https://api.github.com/repos/Vita3K/Vita3K-builds/releases/latest` | `(?i)vita3k.*(win\|windows).*x86_64.*\.7z$` |

---

## ❓ 자주 묻는 질문 (FAQ) 및 문제 해결

**Q1. `config.json` 설정이 초기화되지 않나요?**  
A. 프로그램 빌드, 배포, 업데이트 시 사용자의 라이브 `config.json` 파일은 절대 덮어쓰거나 수정하지 않습니다. 언제나 안전하게 보존됩니다.

**Q2. 돌핀(Dolphin) 업데이트 시 403 오류나 조회가 실패하지 않나요?**  
A. v1.3.0부터 공식 Dolphin CI Buildbot REST API를 직접 연동하여 봇 차단 및 CDN 보안 챌린지와 무관하게 0.2초 이내로 100% 정상 조회됩니다.

**Q3. 다운로드 중 에뮬레이터가 실행 중이면 어떻게 되나요?**  
A. Process Lock Guard가 실행 중인 프로세스를 감지하여 덮어쓰기 파손을 방지하고 종료 안내 메시지를 띄워 안전하게 보호합니다.
